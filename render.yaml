services:
  - type: web
    name: orthotrack-app
    runtime: python
    pythonVersion: "3.11"
    buildCommand: pip install -r requirements.txt && pip uninstall -y opencv-python opencv-contrib-python && pip install "opencv-python-headless==4.8.1.78"
    startCommand: gunicorn app:app
